# Allow Prisma postinstall scripts (needed for prisma generate)
allow-scripts=@prisma/client,@prisma/engines,prisma

# Use legacy peer deps to handle @react-three/* requiring React 18 while Next.js supports React 19
legacy-peer-deps=true
